{
  "name": "test",
  "version": "1.0.0",
  "description": "Project connected to Qase for reporting",
  "scripts": {
    "test": "jest",
    "send-report": "node sendReportToQase.js"
  },
  "devDependencies": {
    "jest": "^26.0.0"
  },
  "dependencies": {
    "qaseio": "^1.0.0"
  },
  "scripts": {
    "test": "jest"
  }
  "scripts": {
  "test": "jest --json --outputFile=./test-results/result.json"
  }
}
